import { RouteShorthandOptions } from 'fastify';

export const rootReqValidator: RouteShorthandOptions = {
  schema: {
    response: {
      200: {
        type: 'object',
        properties: {
          pong: {
            type: 'string',
          },
        },
      },
    },
  },
};
